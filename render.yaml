services:
  - type: web
    name: novax-ai
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py web
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: HOST
        value: 0.0.0.0
